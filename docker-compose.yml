services:
  planni-frontend:
    image: coldblade2000/planni-frontend:latest
    stdin_open: true
    container_name: planni-frontend

    ports:
      - "43500:43500"

    env_file:
      - .env.${ENVIRONMENT}

    networks:
      - red_planni

networks:
  red_interna: { }
